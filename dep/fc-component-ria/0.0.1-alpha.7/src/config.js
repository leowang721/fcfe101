/**
 * @file 基础配置
 *
 * @author Leo Wang(wangkemiao@baidu.com)
 */

define(function (require) {

    var config = {
        ELEMENT_MARK: 'fc-component',

        MULTI_REGISTER: 'ignore'
    };

    return config;
});
