/**
 * @file 模块 `entry/404` - Action配置
 *
 * @author Leo Wang(wangkemiao@baidu.com)
 */

define(function (require) {
    var confList = [
        {
            path: '/404',
            type: 'entry/404/Action'
        }
    ];

    return confList;
});
