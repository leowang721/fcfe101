/**
 * @file 模块 `entry/index` - Action配置
 *
 * @author Leo Wang(wangkemiao@baidu.com)
 */

define(function (require) {
    var confList = [
        {
            path: '/entry/index',
            type: 'entry/index/Action'
        }
    ];

    return confList;
});
