/**
 * @file 模块 `entry/demo/hello/dialog` - Action配置
 *
 * @author Leo Wang(wangkemiao@baidu.com)
 */

define(function (require) {
    var confList = [
        {
            path: '/entry/demo/hello/dialog',
            type: 'entry/demo/hello/dialog/Action'
        }
    ];

    return confList;
});
