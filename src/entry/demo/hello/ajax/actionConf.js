/**
 * @file 模块 `entry/demo/hello/ajax` - Action配置
 *
 * @author Leo Wang(wangkemiao@baidu.com)
 */

define(function (require) {
    var confList = [
        {
            path: '/entry/demo/hello/ajax',
            type: 'entry/demo/hello/ajax/Action'
        }
    ];

    return confList;
});
