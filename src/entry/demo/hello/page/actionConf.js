/**
 * @file 模块 `entry/demo/hello/page` - Action配置
 *
 * @author Leo Wang(wangkemiao@baidu.com)
 */

define(function (require) {
    var confList = [
        {
            path: '/entry/demo/hello/page',
            type: 'entry/demo/hello/page/Action'
        }
    ];

    return confList;
});
