/**
 * @file 模块 `entry/demo/hello/project` - Action配置
 *
 * @author Leo Wang(wangkemiao@baidu.com)
 */

define(function (require) {
    var confList = [
        {
            path: '/entry/demo/hello/project',
            type: 'entry/demo/hello/project/Action'
        }
    ];

    return confList;
});
