/**
 * @file 模块 `entry/demo/hello/component` - Action配置
 *
 * @author Leo Wang(wangkemiao@baidu.com)
 */

define(function (require) {
    var confList = [
        {
            path: '/entry/demo/hello/component',
            type: 'entry/demo/hello/component/Action'
        }
    ];

    return confList;
});
